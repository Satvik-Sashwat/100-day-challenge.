#include <stdio.h>
#include <math.h>
int main()

{
float root1,root2,D,b,a,c;
printf("enter your value of b,a,c");
scanf("%f %f %f",&b,&a,&c);
D=(b*b)-4*a*c;

if(D>0)
{
 root1=(-b+sqrt(D))/(2*a);
root2=(-b-sqrt(D))/(2*a);
printf("%f %f",root1,root2);
}
else if(D==0)
{
root1=(-b/(2*a));
printf("only 1 root %f",root1);
}
else
{printf("_Imaginary roots");}}

